"use strict";Object.defineProperty(exports,"__esModule",{value:!0}),exports.getStream=void 0;const getStream=async function({link:link,signal:signal,providerContext:providerContext}){try{const data=(await providerContext.axios.get(link,{signal:signal})).data,$=providerContext.cheerio.load(data),streams=[],promises=$(".button2,.button1,.button3,.button4,.button").toArray().map(async element=>{const title=$(element).text();let link=$(element).attr("href");if(title.includes("GDFLIX")&&link){const gdLinks=await providerContext.extractors.gdFlixExtracter(link,signal);streams.push(...gdLinks)}const alreadyAdded=streams.find(s=>s.link===link);!title||!link||title.includes("Watch")||title.includes("Login")||title.includes("GoFile")||alreadyAdded||streams.push({server:title,link:link,type:"mkv"})});return await Promise.all(promises),streams}catch(err){return[]}};exports.getStream=getStream;